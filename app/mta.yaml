_schema-version: "3.2"
ID: sbtexp.sbtexpui
version: 0.0.1
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
        - npm run build:workspaces
modules:
  - name: sbtexp.sbtexpui-destination-content
    type: com.sap.application.content
    requires:
      - name: sbtexp.sbtexpui-auth
      - name: sbtexp.sbtexpui-html-repo-host
      - name: sbtexp.sbtexpui-destination-service
        parameters:
          content-target: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: sbtexp.sbtexpui-html-repo-host
              ServiceInstanceName: sbtexp.sbtexpui-html-repo-host
              ServiceKeyName: sbtexp.sbtexpui-html-repo-host-key
              sap.cloud.service: basic.service
            - Name: sbtexp.sbtexpui-auth
              ServiceInstanceName: sbtexp.sbtexpui-auth
              ServiceKeyName: sbtexp.sbtexpui-auth-key
              sap.cloud.service: basic.service
              Authentication: OAuth2UserTokenExchange
    build-parameters:
      no-source: true
  - name: sbtexp.sbtexpui-ui-deployer
    type: com.sap.application.content
    path: .
    requires:
      - name: sbtexp.sbtexpui-html-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - dist/sbtuiapp-content.zip
          name: sbtuiapp
          target-path: resources/
  - name: sbtuiapp
    type: html5
    path: sbtuiapp
    build-parameters:
      supported-platforms: []
resources:
  - name: sbtexp.sbtexpui-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-name: sbtexp.sbtexpui-destination-service
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        version: 0.0.1
  - name: sbtexp.sbtexpui-html-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
      service-keys:
        - name: sbtexp.sbtexpui-html-repo-host-key
  - name: sbtexp.sbtexpui-auth
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
      service-keys:
        - name: sbtexp.sbtexpui-auth-key
